# Reserved for shared AI pipeline configuration.
#
# Paths and hyperparameters currently sit next to the code that uses
# them: feature/target columns in preprocess.py, model settings in
# trainer.py, output paths in train.py. Centralise them here if the
# pipeline gains a second entry point that needs the same values.
